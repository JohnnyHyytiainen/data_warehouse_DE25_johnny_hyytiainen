---
title: "Modern data stack   using dlt to extract and load api data to snowflake"
video_id: 5fur2ZTonDQ
source_url: https://www.youtube.com/watch?v=5fur2ZTonDQ
language: en
transcript_type: auto_generated
fetched_at: 2026-09-13T14:09:32+00:00
source_type: youtube_transcript
---

# Modern data stack   using dlt to extract and load api data to snowflake

**[00:00]** Hello and welcome to this video where we'll use DT to extract and load API data into Snowflake. This will require us to make different roles. We'll make a user. We'll create that we give the proper grants and privileges to our roles and user. And then we will take a look into the API doing some exploratory data analysis on the API. When we have done that, we will go into loading this API data into snowflake and then we'll go into snowflake through snowql and also in snow site to check out the data. This is still extract and load part of the elt. Afterwards, we have the DBT for transformations. We'll go into the browser first.

**[01:02]** Here I am in the code repository and we will work with RBS permitting API. This is a Swedish data for job ads and we'll go in there and take a look. This is the page. However, I will also note here before I jump out is that we need to use the DT the secrets. to set up the connection to snowflake. We will go through that as well. Remember to note this down cuz this we will use later. And then I will go into this page here. Let me close this. Code examples start here. Here we have job search examples.py. Here you have the code to get started and basically get ads and then using these examples and then you can query on different jobs. We will use this first

**[02:00]** to explore the data and then we will turn this into DT later on that we can connect it to load the data into snowflake. Let's move on to Visual Studio Code. Now here I am in Visual Studio Code and let's start up with virtual environment bin activate and then pip install. We need to have TLT snowflake. Okay. And then we need to have IPI kernel for Jupyter notebook requests. We need to get the data from the API. Okay. This is done. And now we will create terminal. I will create make dip DT code where I'll place the DT code. I will have worksheets snowflake and this is it. I will also do touch.git ignore.

**[03:01]** Inside of git ignore I will make sure to remove DLT because we will have our secrets over there and we will also get rid of TLT setup user. Okay, now it's time to go into CD worksheet snowflake and inside here I will create a few SQL files. I'll create setup setup database.sql setup user.sql and then I will have check loaded data.sql setup roles.sql if you have watched the video before it's a kind of similar setup. You need to create a database inside of snowflake. You need to create the schema. You need to create the roles. You need to create the user for DT. And

**[04:02]** then you give the grants to the roles for different things. For example, the warehouse, the database, and the different select and different operations to different roles. And why I do getting in setup user is because I want to place my password there. And we shouldn't push the password up to GitHub if you're working in GitHub repository. This is not a GitHub repository, but I put it in Git ignore here to make sure that when you're watching this and following along, you don't push up your secrets there. And also same with DT because there we will have our secrets. Now I'm back into I will do CD DT code. And inside of DT code, let's touch a few things. Touch I want to have a this is the Jupyter notebook that we will use for exploring the API. We will have load job

**[05:01]** ads.py that this is the pipeline. You can see here that they exist now. And we will also do T and in not DTL RM DT and then we will do touch and DLT/ secrets dot okay let's start here let's go back to our browser and let's get back here and I will go into this copy this part here this is the DT's connection to Snowflake. Paste it in here. I will call this job ads, this database. And the username is extract loader. You need to create this user if you haven't done that, but I'll show you how to do that. And then this password. I will pause my video and I will set this up. And also the host you find in your account details inside of Snow Site. You click

**[06:02]** on your account and you click on account details. you will find your account identifier. That is what you need. The account identifier you paste it in. It's basically a few capital letters and then dash a few capital letters. Okay. And then the warehouse which is dev warehouse. This is the one I created. But if you don't have that, you should either create that or you can choose another warehouse that you have. And the role that we have is job ads DT ro. This role doesn't exist yet either. This we will create in our setup roles and the database will create that's called job ads and also the user will create this extract loader. Now I'll pause the video and I will set my own password and my own host. We'll start with setting up the user and then for that I will use the user ro user admin.

**[07:00]** Note that I'm logged into my snowsql account here in the snowflake extension. I'll run this one. I'm using user admin and then is create actually I can copy this one from the lecture notes here create user if not exists extract loader. Note that this one I plac in this setup user and the setup user is get ignored. And then I will pass my video to to set up the my password and I will use the same password as I have in secrets.2 mode. This is very important otherwise it won't work when you're loading the data later on in DL. Remember to run the create user after you have put in your password. I have already done that. That is finished. Now it's time to set up the database. And to do that I will use the role. Let's see. Use ro sis admin run this one and then I will do create database if not exists

**[08:02]** job ads I'll create this database and then I will do create schema if not exists job ads dotstaging. Okay, now we have created that one and it's time to set up the roles. I'll go into set up the roles and change language to snowflake SQL. Now we will create this roles use ro user admin. I'll run this one. Create ro if not exist. And what do I want to create? I want to create job ads dt ro. I create this ro. And then it's time to use I'll change the role to security admin. This is the one that will handle the grant, right? Grant the roles. Grant

**[09:00]** roll job ads do roll to user extract loader and also I will grant this to myself. job adds DT ro to user coction. Okay, I grant it to myself and now it's time for us to grant and I won't I will just copy this one as I have done this all before. I copy all this and I will explain it to you. First we grant usage on warehouse dev warehouse to roll job as DLT. It needs to have the warehouse in order to do the compute, right? Grant and then we need to grant usage on database job ads that it can access this view this database and then we need to grant usage on this staging schema and then we grant create table on schema.staging staging in inside of staging it can create tables which is needed for DT and then

**[10:02]** afterwards I will do I will do CRUD operations it can it I will give it all of them select insert update delete on all tables in the schema and then grant select insert update delete on future tables in schema in in the tables that we will create later on it will still get it it will still be able to do this within the staging schema not in other schemas. We don't want to give it more privileges than it should have. Okay. And with this I want to run all of them. Click on this one. This will execute from beginning to end. Okay. And now it's time to check our grants. I will copy that as well. Show grants on schema and job ads. The staging you can see create table usage and ownership. On the schema on the

**[11:00]** schema you have these things and then in schema you can see we have delete, insert, select and update. The crowd operations on all the tables within this schema is gi this schema staging is given to this uh granted to this job as DT role and then show grants to this role and what you can see is that it has usage create table usage on these different database schemas and warehouses and then let's do show grants to user extract loader and you can see that the Extract loader has gotten this job ads DLT role and also you can do show grants to the user that you used yourself. I used coction as the it's my user. You can check that you also have this role that you can see and work with the tables that DT will create for us later on.

**[12:00]** Okay, this is setting up the role and now basically we have set up a database, we have set up the roles, we have set up the user and what we need to do now is to explore the data. Basically, in order to explore the data, we have here I'll call this EDA on job tech API because this is what it's called. And basically what we'll do first is that we'll go into the browser and inside here this is the code that we will have. I'll copy this code. Basically I'll copy this code. I'll go back here. Paste it in. Change this to Python. Let's see. Python environments. VT extract load. Yes. Okay. I will remove that one and just run this and see what we get and then I will go through it a little bit.

**[13:01]** Okay, run it and we can see that we get a lot of teachers here. This is in Swedish loops blah blah blah because we have queried Loopsala. What it does is that we do example search loop through hits query. It goes to here into this one and here we have search parents the query and then we have the limit how many how much limit we want and then the JSON response is basically this underscore get ads function you can take a look into this get ads what it does is that we have the response here is the request get and you have to send in this headers to get the back the JSON and you put in the URL for search and the different params that you have this is params that you send in and you send in the search params and then you do response this is to check for errors and then you have JSON.loads basically you get this back into JSON data. This is basically how

**[14:02]** you get the data from this API and the URL for search you can see it up here is this URL here is basically this URL and then slash search. Okay. And with this one you can also do this example search loop through hits and it will go through the hits and you will get to print out the headlines hit employer and name. Now we have 11 hits but let's move on a little bit. We can test out a few functions. Example search return number of hits. We can do set in our query and what you can see we get number of hits is 11. What else can we do? We can go into we can take a look into the search. We can go and copy these ones here.

**[15:01]** This is the search params and this is the JSON response. And you can take a look into the JSON response basically. And this JSON response, you can see that it's a dictionary. You can take a look into that one. Type is a dict. And if it's a dict, you have the keys, right? Keys. And then the keys, you can see there's total, positions, query time, result time, stats, free text, con concepts, and hits. Okay. Then we can take a look into different things. We could do JSON response get hits and then you see we get this is a similar you get a list of the number of hits. This is a list. You can do take a list. You can not take a list but length of this one and you can see it's 11. It's same as the one that we had before

**[16:00]** where we have the number of hits here. Okay. What else can we do? We can do we copy this one. This gets all the hits and we can take a look into one hit the first one or the second one if it's counting with starting with zero. You can see here we have one one school here. What we can do here is since this is a dictionary you can see that with this one curly braces and you can see key and value pairs. Then what you can do is dot key to check out what's inside here. Yes, there's a lot of different things inside of hits. We have keys, right? And then we can take a look a little bit further going in here. We can take a look into the headline for example headline and we can see Jensen gymnosium. Okay. And we can continue here. We can take a

**[17:02]** look into we go up here and copy this part here to get the to search for something else. And then you can see the query. Let's change the query a little bit. And we can type it out here directly. I will search for data engineer because this is our field. And for data engineer, we can do get ads on that one. And also you can see we can place this directly into this one here. We don't need to divide it if we don't want to. And then we can take a look into JSON response. Okay, a lot of jobs 78 I guess and 99 positions. Okay, great. Let's see. Dot keys. And it's the same as before. What you can do now is that for example we can take a look into JSON response of hits and with a list we can take out minus one to get the last one and we can

**[18:03]** take out the headline and we can see okay senior software engineer data and video analytics learned. Okay, great. And we can take a look into I'll copy this one and take a look into their employer because that was one that we saw before. That is one of the keys. And inside of the employer, we see here phone number. We see email, URL, organization number, name is this axis communications. And what we can do is that we can pick out the names if we want. We pick out the name. Okay, great. Now you can see that okay, it's a senior software engineer for this company. Okay, this concludes the EDA that we need. And now we'll go into the actual DT code where we'll use this that we

**[19:00]** have learned from the EDA to actually load the data from this API into Snowflake. Let's move on to load job ads. The DLT, we go in here and we import DT. We import requests. We need a requests, right? We import JSON and we take from path liib import path and then import OS. These are just a few imports that we need. And then now we will go back to API key, our API EDA. And here we have this get ads. This is what I want. I will copy this one. Actually, I will copy all these actually. Yeah, I'll copy this. Go back in here. Paste it here. First, we have this get ads. And here I

**[20:00]** will put in URL for search that you can get different ads depending on what you want to search for. Okay. And then we will create our resource at dt.resource. This is a way of grouping several data yielding they where we get data from different resources are the lowest unit and they can be combined into a DT source which we will take from the source or the resource to the target and the target is the destination which is snowflake in this case. We take DT resource and we can choose the right disposition here and I'll choose replace as the simplest one but there's also append and merge for doing other types of writes depending on what you need you might be

**[21:00]** able to be interested in using the other ones def job ads resource and here we will take in params and we will take these do I'll move them down in here and move them without down button or option down button if you're in a Mac. Here we have URL and URL for search. And now I will take for add in underscore get add. We take this function, we put in URL for search and put in the params, the different parameters that we have. The parents will be sent into here and the params will be sent into this function get ads and we'll use the params here and for our get request. Okay, after we do that,

**[22:00]** we remember that when we get the ads, we need to go into hits in order to get our jobs, right? I will take get hits. It's a dictionary. We do get hits. We get the hits and then we yield the ad. Remember that when we go into hits, we get a list. And then we yield the ads. We send out one. We send one, we return one ad at a time, one item at a time. That is what yield does. It's yield instead of return, which will return everything. This is a generator. I won't go in exactly what that means, but this is what you have here. You're yielding each ad when you're calling this. And this is required by our pipeline later on. Let's create our pipeline. I will change it a little bit now. I will put it put the pipeline before in in another video before we put it into

**[23:02]** our script directly in if done the name equals standard main but now I will create a function of it I will call it run pipeline send in the query and table name that you can customize it a little bit and then pipeline equals to DT.pipeline pipeline and here we have pipeline name equals to job search and we have destination equals to snowflake. Remember that this destination it will look into our secrets.tuml and inside there we had specified snowflake destination and the configuration for that. Okay, after this part we should have data set name also which is the schema staging schema or staging layer. This is where our data will land

**[24:01]** and then after we have this we will you take the params equals to remember that the params is defined like this. We have Q for our query. What whatever we type in here for a query. For example, we want to search for data engineer. And then we put in data engineer here in this query. And we have a limit here. I'll make it default to 100. And I won't go through how to do pagionation here, but basically you can do pagionation and work with limit together with offset in order to get more data. I have that in the repo if you're interested in you can take a look into that but I won't go through it here how that works. I also have another video where I do basically the same thing where I have the the pagionation implemented. However, instead of snowflake we used the we use duct db.

**[25:00]** You can take a look into that video. I will link that in the description. Take a look into that if you're interested in how to do pagionation. Okay, but I won't do it here. We have the params. We'll do load info equals to pipeline run. We run the pipeline and we put in when we run the pipeline, we need to put in the resource or we need to put in the source. We put in job ads resource. So you can see data is the first one. Job ads resource. This is our data. We place in params equals to params. Look at this job ads resource. It requires this params. We send in this params with query and limit. It gets these params and then it will send this into this get ads and we will return back the hits based on the query basically like that. Okay.

**[26:01]** It means that we have this load info which runs the pipeline and then we print this load info and that is it. And now we can do if done the name equals to done the main. And I will do I need to do working directory equals to path then the file dot parent working directory. This is the dunder path.p parent. We're into this file parent. We go into DT code. But here, this working directory, you will be able to see DLT. That is because when we click on the run button, it it runs based on where you are in the terminal. If you're in this root up here, then you won't find your secrets or you won't find your DLT. That's why

**[27:01]** we have to create this working directory. And then we need to do this OS change did. And now I will do my query. I will call data engineer and table name. I will call this data field job ads. This is the table name that I will send into my run pipeline. I send in query is data engineer. Let's say query query, table name. And then we need to use the table name here. And what we do is inside our pipeline we can do table name equals to table name. And basically is that we okay we have data set name this is the schema the staging and inside this we will create this table with this table name here that is data field job ads we will get this table basically I save this one and

**[28:02]** I'll run this one and then it takes a little bit of time. Okay, after it has run, we have pipeline job search load step completed one load packages were loaded destination snowflake and into data set staging

**[29:00]** job as location load package. Okay, we can take a look into our loaded data. Now we'll go into our check loaded data and I'll change the language to snow snowflake SQL and we will use the let's see use roll job adds DLT ro and run this one and then we'll have use database case and job ads. Run this one and I will show schemas. You can see which schemas we have. Yes, we have information schema public and staging. Okay, we can show tables

**[30:01]** in schema staging. Then we have a lot of tables here and you can see we have we have a lot of tables to see data field job ads must have education level etc. And we have here data field job ads. There's a lot of data that has been loaded. Oh, sorry. What you can do is that you can select on all the different ones to see what data has been loaded there by DT and see which data that is relevant for you which you can work on later on. This is one reason why we place in everything. We dump the data into staging and then in the staging layer you pick out the data that you want and you go into the next layer which is the refine layer and where you can do your transformations. Here I want to just

**[31:03]** take a look into this staging dot data field job ads it's called I'll describe table take a look into this one and you can see oh this is the ones that we get from the hits we get relevance ID label webpage URL headline etc description text there's a lot of a lot of different columns here then we can use let's see we can do select headline employer let's see what we can pick employer here let's see employ let me see employment type here we have employment type yeah there it's not alphabet atically

**[32:00]** but yeah we can just employer workplace maybe there should be one if they haven't changed it from staging dot data field job ads run this one yes it works here we have employer workplace what you can do more is basically for example description text yes and you can see here is the description Formulate H&M job description. Okay. Data engineer here. Tell it too. Yeah, it's a little bit longer description. Good. We can do Yeah, basically here you can do a lot of things. We can perhaps pick out one one employer for example. Okay. ATG. Do we have more things there? Select star from staging dot data field job ads

**[33:02]** where employer workplace equals ATG maybe. Yes. And then we only had one, right? Yeah. One data engineer. Okay. Now you can work with this as you want. I will save this one and I will show you that the data actually have been loaded into snowflake. Inside of snowflake or snow site, we can see that we have this data field job ads under this is the job ads database and here we have the staging and we have a lot of tables. You can see the data preview here. It will take a little bit time for it to load. I can see this because I've assigned the role to myself and here you can see the different things that we loaded. We can

**[34:02]** see here the one that we got before data engineer at reflex this description text etc. And you can see there's a lot of columns here, 66 columns. You can take a look into that one. And what DLC does is that you can see how it has nest. Basically, you have nested data, right? You have a JSON object with a lot of nested. And in DLC, it's possible to load based on the level that you want. But when it goes even deeper level, you can see if you drag it out here that it's continuing another nest here. Data field job s and then underscore application contacts. Actually, this one could be very useful for you if you want to uh you could join the tables later on. Here you can get the different emails for different people that if you want to apply for these jobs, you can take their emails and their phone numbers and you can use it. And also here you have for example here's another

**[35:02]** one that is quite nested. You have here education and you can see the data preview but there's not much data there. This is also something you could take a look at and go through each of them. In this video, we have used DT to load the data from the jobtech API and we have extracted and loaded it from the API into Snowflake and then we went into working with SnowSQL to actually test it out and saw that yeah, we got data, we could query on the data and then we went into Snowite and checked that the data was there. But before this we needed to set up the database the roles in we use snowsql but you could do it in worksheets in snowflake in snowite that will also work but yes we have gone through the e part of the elt. This is the start of your pipeline where you ingested data from an API. This is super

**[36:01]** cool. Thank you for watching this video and hope that you have started to get interest in modern data stack and continue to learn more. See you in the next video. Bye.
