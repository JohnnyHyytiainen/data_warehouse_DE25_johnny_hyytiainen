---
title: "Modern data stack - Using data load tool (dlt) to ingest CSV data into Snowflake"
video_id: 3zrs7CBPjaE
source_url: https://www.youtube.com/watch?v=3zrs7CBPjaE
language: en
transcript_type: auto_generated
fetched_at: 2026-09-13T14:07:24+00:00
source_type: youtube_transcript
---

# Modern data stack - Using data load tool (dlt) to ingest CSV data into Snowflake

**[00:00]** Hello and welcome to this video where we'll use DLT data load tool which is a lightweight Python code to move data. So we will use this to ingest the data from CSV files and move it into Snowflake and then we'll go into Snowflake in Snowite or SnowSQL and check that we have data there. Using DT is the first step of the ELT process. extracting the data. So we extract the data from different sources. One source is CSV in this case. Other source could be API. It could be from another database and then you load it towards a destination and the destination is snowflake. So we extract and we load the data into snowflake and then afterwards we can connect like dbt to transform the data. So you have elt right. So this is the pipeline and then after you have transformed the data you

**[01:01]** could serve it in March so that the BI could use it afterwards business intelligence dashboards for example or you could serve it for AI. So this is the modern data stack and DT is a crucial part of it but you could replace DT with something else but I like DT and I will use DT in this video. So we will start in the browser and in the browser we have the repo here and this is we have data and also there's a folder that is not pushed up to the repo since I have git ignored it and it's called DLT and inside this DLT there's secrets to ML tommo so that file is used for keeping the secrets to the connection details to snowflake so I will show you how to create these details soon. And then the data we have here is Netflix originals.csv.

**[02:02]** This is how the code looks like and I will just explain it briefly. So this is a Python script and what we have here is we start with some imports and we have here is a load load CSV resource. So the you make it into a resource so that you can group different types of resources together and what it does is that it yields the data. You can choose different disposition of so for example replace there's also append. So you can try that out. So you append the data in the table and also merge. So you merge with the data you have before. And then another part is this working directory. The idea I'll increase this a little bit. The idea is that I choose the working directory as this file. So we get into this DT code here. So this part and then what we do is os.

**[03:03]** So ch did so we change it to this part here. so that we can access so that the DT can access this DLT and then secrets.t2 Trumal that is the idea and then you have another part here is the pipeline and this is what creates this movies pipeline. So this is the name and it will be stored in DLT and then there is a pipelines folder and then it will store this pipeline and here the destination it says that okay we will load it into snowflake and then we have data set name this is the schema that will be created and we'll create a schema called staging where the data will be loaded basically in snowflake and then you have this pipeline run here and load info. You'll get some info on how the loading has gone. So these are

**[04:00]** the few steps to create the DT pipeline to load CSV data. So that was the DT pipeline. So something else to look for is that before you create the DT pipeline, you do need to set up different things in snowflake. So we need to set up database first. So here you create the database movies and then you create the schema staging because these needs to be in place before you load the data right because you need to know where the data should go and we have specified it should go to movies and we have specified it should go into this staging layer. So then you have to create this first. They need to exist and then setup user role. Uh here we will create the different roles and then we will make sure that all the grants are correct so that they get the correct privileges. I will explain a little bit shortly here is that we need to grant usage on the warehouse database and

**[05:02]** schema to the DT role which you create. You create a DT role and also you will create a user. So now it looks like this. But I will make sure that this will be put into different files so that you don't share the password in your repository. That is very important. And then we create the DT user. The DT doesn't need to have read. It needs to have insert, update, and delete on all tables and on all future tables as well. And then you create a movies readers role and give it access to this warehouse database and schemas and run select on it and then finally grant the role movies reader to your own user. Finally before we actually code this out we can take a look into the readme here. And if you scroll down a little bit here is what you need to install. And then

**[06:01]** here is the DT's connection to snowflake. So this is what you need to do. You type in this destination. Snowflake credentials and then you have here database is the movies which is the one that we have created, right? And then username is extract loader the user and then we'll have here the password that you need to set up and the host this is the account identifier. So this one you find in snowite. You go into snowite and into your own account and then you will find this and it will look like this. So I'm in a empty visual studio code. So I will start with installing stuff. So uvnv to create a virtual environment source then bin activate. So activate it and then uvp pip install. What do I need? I need to have DLT DLT snowflake

**[07:01]** and let's see I will need IPI kernel to work with Jupyter notebook to show you a little bit there and then I will need to have DT parket to work with CSV files and I will need to have pandas as well. Okay. So then I've installed everything I need and then I will have my data folder and here I will place the CSV file and I will have DLT folder and inside my DLT folder I will have this secretstomal and then I will also have my worksheet snowflake And inside there I will have my SQL to set up the database to set up the different roles and grant privileges.

**[08:01]** And you note that this can also be done in snowite but we will use noSQL for that and let's see in secrets. Just paste in what we had in in the repository. So I will copy this one. I'll paste in here. And then we just fill out the different things. And what needs to be filled out here is the account identifier. So I will pause the video and fill this out. And we will need to fill out a password. And I will pause the video to fill out that one as well. And now it's time to set up the database. So we go into the worksheet. And I will create here setup database SQL like this. And let's see setup database. So here we will use the role sys admin

**[09:03]** and then we will create. All right. We need to change this language mode to snowflake. I need to log to snowflake as well. So log sign in here and then I will have MFA. So it will prompt for MFA and then I log in here. Okay, good. Okay, use this admin and then create database if not exists movie and then create schema if not exists. Movies dot Let's see movies.staging. So we create this staging schema and I run this one with command enter or control enter. I run this one. Movies already exist. That's because I have done this preparation before. So I'll just drop it. Drop database movies.

**[10:02]** Run this one. Okay, it has been dropped. And then I will create this one. Okay. So, database movies successfully created and staging successfully created. Okay. So, if you go into Snowite, you will see this one. You can also do if you don't want to go into Snowite directly, you can check out the database like this. The scribe database movies and you can see here this is one that we created and there's a staging layer or the staging schema. Okay. And now when we have created this one I want to set the user.sl and this one I will also change to change language mode to snowflake scale. And how do I do that? I do control shiftp or control command shiftp if I'm in Mac and I am in Mac. So I'll do that. Okay. So I'll set up the user.

**[11:00]** Let's see. to set up the user. I put it in my own script here because I want to ignore this if we're in a GitHub repo so that this doesn't show. So what we do is use ro user admin and then we create a user if not exists. Let's see extract loader. This is what I call it. And then I will have a password here equals to the password string. And then default warehouse should be equal to dev warehouse which we have created in a previous video. So if you don't have the dev warehouse, you could use compute warehouse or some other warehouse that you have available.

**[12:02]** So this is just a warehouse that I created in an earlier video. Okay. And the password string I will pass this video and I will place it in. But I will start with running this one and then you run this one. Now I'll also show you ignore. So I will just create a new gig ignore because this is not a repo per se. But if you when you're working with a GitHub repository, you should definitely do this. So start with DLT so that it will remove this DLT folder and then you should remove setup user.sql so that this one doesn't come up in your because you have your credentials there, right? So you don't want to send it up to GitHub. Okay. And don't merion control it. So now you have Git ignored. And now we need to set up the different roles. And I'll change the language mode

**[13:01]** here to snowflake. So we will use I'll just copy some code here. So use RO user admin. So I will use this one. And then I will create this RO movies DT roll if it doesn't exist. Okay. So I have it before. So I'll drop it actually. Movies DT roll. I'll remove that one. Let's see. Can't I do that? Drop roll. Yes. Okay. So it's dropped. So I will create it from scratch. Movies DT roll. I'll also drop another one. Drop roll. Movies reader. I'll call it right movies reader. Okay. So I drop this one. So now I've created the ro movies DT ro and we will create also another one. I will create movies in reader ro.

**[14:03]** Okay. So the idea is that the DT doesn't need to have the read operation. So it doesn't need to have select while it needs to have insert, it needs to have update, it needs to have delete. Okay. So we need to grant the privileges to this role. And to grant the different privileges, we use RO security admin. So I'll run this one. So grant ro movies DT RO to user extract loader. So it needs to have this role. And also for simplicity I will grant this movies reader role to myself. So to coion and this is in order so that I can see the data in snowflake afterwards. So here we grant to users and then afterwards we need to grant some privileges to the roles. So you can

**[15:03]** see that I'm copying this and not typing this out. That's because I've already typed it out and explained it in roles management. So rback. So if you want to know more details, you can go into watching that video. So it's similar to this but it's different roles and different uses. So here we grant usage on warehouse dev warehouse to movies DT role. We grant usage on database movies to ro movies DT ro and we grant usage on schema movies.ststaging to roll movies DLT roles. So we grant it usage to the staging schema because the staging schema is the only one that it needs and it needs to have this warehouse so that we can do some computing. So the dev warehouse. So run this one. Let me run this one. Let's see. Syntax error. Y usage on dev warehouse. We are using

**[16:04]** security admin. That's correct. We run this one. Okay. Yeah, it works. I run this one as well. And then I run this one. And I run it with a command enter or control enter and or pressing the execute here. Okay. Okay, so we have given it some privileges and then we need to give it able to create table inside of the movie staging and give it all the crowd operations without read. So cud operations. So it can insert, update and delete on all tables and on all future tables in the staging. And it should be able to create tables because we will use DT to create tables. But it doesn't need to have read. So run this one, run this one and then run this one. Okay. And now we should check we can check the grants. I will copy here from the code.

**[17:03]** So check the grants. So show grants on schema movie staging. You can see here on staging. So ownership. Who has the ownership? Well, it's the SIS admin. Yes, because it was the one that created this schema. And we have here usage granted to this ro movies DT ro and it can create table this movies DT ro can create tables. Great. Great. And then we can show future grants to this staging and you can see the different grants it has. So it has been granted this database this schema movie staging. It can create table and usage on this warehouse and then show grants to user extract loader. We're granted this DLT role to this user extract loader. So that is the idea. Now we will do similar let's see which

**[18:01]** role show current role. I need to have semicolon show current select current role. It's called select current role. Okay, I'm using security admin. Okay, good. Then I can continue using that one and I will do similar thing but I will grant usage on warehouse movies staging to the movies reader and the reader should also have the select. So you see select in moviesstaging and select on all future tables in database. So I will run this one. movies reader does not exist or not authorized. Let me see if I run this one. What happens? Okay, let's see why. Okay, so the reason is that if you scroll up, we created movies reader roll. So then we need to have roll here as well. So roll and roll. So now if I

**[19:01]** run this one, it should work. Yes. Run this one, it should work. Run this one and run this one, it should work. Okay, good. And also there's a possibility if you have just copied all the code from me and then you can just click this one to run all of them. So execute all statements also possible. Okay, now we have set up our roles, we have set up our user and now it's time to move to DT. So for DT it's quite simple. We have we start a new file here that I will call or we could do like this. Yeah, we could actually do like this. We can create another folder called DLT code if you want to separate them. But if you do this, you need to move this DLT inside of it. So I move the DLT inside of DT

**[20:00]** code. And then I will create a script here. that is called load csv.py load csv.py pi. So note that note how it looks like. So we have DT I can actually show it here. Tree dot. So we have data here. We have DT code that is next to data and that is fine as long as we know what we're doing later on with with path and path li. We have worksheet snowflake here as another folder. So inside of load CSV, here's our DT. So first you need to do import DT and make sure that you go in to the bottom right to choose that this is using our virtual environment

**[21:02]** and then import pandas as PD. We need to have from pathlib import path and we will need to have os. So we start with dt.res resource and what this means as I said before we group different types of data yielding resources into the resources and then you can place several resources into something called source which you can use later on. So this is where you get your data basically. So resource is the lowest unit and several resources go into the source and it's the source you get the data and then you place it in you put it into a target later on which is a destination. So from source to destination that is what you need to keep in mind. So for here we do a right disposition and the right disposition you can choose different ones. You can

**[22:01]** choose here appel merge or replace and also skip. But I will show you replace. This is the simplest one. And what it does is that if there's data there it will just replace it. For append it's if there's some new data you will put it in otherwise it won't put in. Okay. So we have resources here and we can do defaf load CSV resource and here we'll have a file path and this is a string and I will also put in quarks. So these are basically some keyword arguments that I can put in and I just need that for my data frame here. So df equals to pd readad csv and we will put in the file path and we'll put in our quarks. So if I need to put in some arguments for my data frame basically for pandas when it's reading in then it will do yield df

**[23:02]** and what this yield means is that it will return data one by one and it will exhaust the data. So instead of having a function that will return all the data at once, it will return data one by one. So that is the idea with this yield statement. So it creates a generator function it's called. And a generator function can be exhausted. If you don't really understand what I mean by this, I will actually create a video about generators later on in the future and explain what that means. And I will put a link in the description. So that will be part of my Python course or maybe efficient Python course. So then I will talk about generators. But uh otherwise you can see it as just boilerplate code. Maybe you can see it as instead of return we have yield in in DT. So that is what you can see it as.

**[24:01]** Otherwise it's not it's actually it's not that difficult but it's another concept but it's I don't want to put it into this video. Okay. So we have our resource and now we will have if done the name equals to done the main. So this will run the code when we run this script directly. So working directory the working directory is path done. So this is the working directory. The DT code and we will also make a data path equals to working directory is DT code right and then we will do dot parent we will go if we do parent we will come up to DT extract loader and then here we'll go down to the data folder. So

**[25:01]** concatenate with the data folder. So now we have the data path and we have the working directory or we can call it data directory something like this and then we will do os.change did change there to working directory. And why do we do this? Because we need to be in the working directory to see dt so that we can see the secrets.tuml tumal inside of it. That is how DT works. It needs to see DT in the terminal. But if I run click the run button, the Python script is ran but it's run from where I am in the terminal. So if I'm outside of the DT code, then it won't see DLT. That's why I changed the directory to so that I can see DLT. Okay. And then we have CSV path

**[26:03]** is equal to data directory slash what it's called let's see Netflix originalscv Netflix original CSV and that's it we have this one and then we can get the data equals to load CSV resource was note that this is this function here. We put in our CSV path and we can print out the data and I will run this one. So this is running it. I think there is an error, right? Yes, there is an error. So let's see. If you want to see the data items in the resource, you must iterate it or convert it to list. Okay, then I will actually

**[27:03]** do this. Yeah, because it's running actually we need to I will remove this DT resource for now or actually I won't do that simply simpler I will just do print DF here and I will remove the yield and I will remove the I will remove this one and then I will see what happens if I just run this I just want to see if I get the data frame so I click on run okay I don't get the data frame. As you can see, there's an error here. Uni-ode decode error. That's why I need to place in encoding equals to Latin one. Okay. So now if I run this again, we can see the data frame. Okay. Good. Because that is just what I wanted to check. So I can see the data frame. I remove this one. Command K, command U.

**[28:00]** Command K, command U to uncomment. So now this means that this DLT resource will work at least we can read the data frame. So after that we do like this we create this pipeline object. This is a DTO pipeline and here you have pipeline name I call it movies and the destination is snowflake and we have data set name is the staging layer right so this is the schema and afterwards we have load info so this is the running of the pipeline. So pipeline.run data we put it into a table. We call this Netflix. So this pipeline what it does is that it

**[29:01]** it's called movies. It's destination is snowflake. So it will use secrets.tuml that we have which we have snowflake and the credentials for snowflake. It will go into this data set name called staging. And inside of the secrets of tuml, we have the same password as the one we have set up as the extract loader user. And we have the correct role. We have the correct warehouse. We have specified which database we should use. So we go into movies database. Right? And that is the idea. Now we have everything we need. We have the setup in tumal and now we do pipeline.run. We put in the data. So from the source or the resources we will load it into our destination that is snowflake. Yes, that is how this works. And then I

**[30:01]** can print load info and clear this out. And I will run it. And it takes a little bit of time for the data to load. Okay, now it says pipeline movies load step completed in so many seconds and it is loaded and contains no failed jobs. Great. Then we go into worksheet snowflake. We open up a new script check loaded data.sql. We change the language mode to snowflake and see that we have the data. So use ro movies reader role. So I'll use this role. Okay. And because this role can read, we can say show databases. Run this one. And we can see we have a movies database here. And what we can do is use database

**[31:02]** movies. I will run this one. So I use this movies. And then we can do select star from or we can describe it. Desk and describe describe database movies. Okay, we have these three describe. Can we do describe schema staging? Because I have used the database. So I just I can describe the staging and ooh interesting we have something called Netflix. This is the name. We have DT loads, DT pipeline state and DT version. So these three are used for DT to keep track of the metadata so that it knows what has been loaded and what needs to be loaded more in case

**[32:01]** you do merge or you do append and there's a history of it stored inside of Snowflake now. Okay, what we can do is we can do desk actually desk instead of describe desk table and Netflix this is what it's called let's see table does not exist it's inside of staging so I need to use staging Netflix okay or we can do use staging okay and you can see this is the description of it we have we We have title it's a var car so this is a string we have genre it's also a string etc and here we have number 190 and here we have float so different data types so this is the schema that has been loaded so DT creates the schema for us based on what it sees inside of the data

**[33:00]** so you can see a lot of are strings some are number float etc okay so what can we do Now let's take a look into this. Select star from staging.netetflix. Let's see. Insufficient privilege to operate on table Netflix. Okay, I did some debugging and what I did was that I checked into the different roles and made sure that we have set up. I gave all the correct grants. So what you can do inside of setup roles, you can take a look into gran, you can check the future grants on the database movies to make sure that the reader role actually has it. Maybe I forgot to run one. And here you can also check the staging so that it has all the other things for the reader role. So it can see select on this schema. Okay.

**[34:04]** And afterwards you can also this is to give on all tables in the schema that exist. And here this is for all the future tables in the movie. So it will affect all the schemas as well. And also one more thing I did when I debugged I gave here the DT role to myself so that I can go into snowite to actually if go into the table and drop it drop the database also if needed and rerun the Python script the DT but after I did that we can also check loaded data and here you can see that I'm using this ro read a ro And in the end we can do select star from staging.netetflix. And with this we can do different things we want. For example, count star from

**[35:00]** staging.netetflix for example. And you can see how many numbers there are there. So as you can see this one works. And I'll show you in snowflake it looks like this. So in snowside this database we have used that with snowside snowflake snowsql created this movies database and inside of this movies database we have this staging and inside of this staging layer we have these three here so DT loads these are some meta data but actually we don't need to see it we can go into Netflix data preview and you can see the data is So that was really cool. So in this video we have used DLT to load extract and load the data from a CSV file into the destination that is Snowflake. And in order to do that, we needed to set up the different roles and

**[36:02]** the user. And so we set up the movies DT ro movies read the role and we assigned them to the user that we created and made sure that the users the password that we had is the same as the one in secrets. DT. So this is the first step in your pipeline. You want to get the data into your specific destination. And this is quite interesting because if you're not using Snowflake, you just changed it to something else and the DT still works. For example, you don't even need to have any credentials. If you're working with a local file and you can change to BigQuery if you want, you just change the tool. Then you need to work with BigQuery to set up the database, the schema, and the roles. Yes. But I hope that this was helpful for you and thank you for watching this video. We'll see you in the next one. Bye.
