---
title: "Role based access control (RBAC) in snowflake"
video_id: MAMW3cUWQrs
source_url: https://www.youtube.com/watch?v=MAMW3cUWQrs
language: en
transcript_type: auto_generated
fetched_at: 2026-09-13T14:03:40+00:00
source_type: youtube_transcript
---

# Role based access control (RBAC) in snowflake

**[00:00]** Hello and welcome to this video where we've come into roles management in snowflake. This is the coding part. We will go into snowql where it's in visual studio code where we can connect snowflake and then we will create a new database create new warehouse create different types of roles and then work with roles management. We will grant roles to other roles. Grant access to database to different roles. Through that we will use the the roles that exist in snowflake already to grant this access. For example, we have security admin, sis admin, user admins. So we'll use the different ones of them and see the different purposes. This will be really fun. And let's move on directly to Visual Studio Code. This is an empty visual studio code here and start with logging into snowflake snowsql. Here we go in and login and then this one will

**[01:02]** require MFA. I will go into my duo and accept it. Okay, I'm inside of snowql and you can see here account admin, which database, which warehouse, etc. We start with account admin. That is fine. Okay, let's start with setting up. We have set up db.sql. We set up our database. Starting, we need to change this one. This is actually a dbt file, but I will change the language here. You do shift command P or shift control P to get here. And you search for change language mode. I change language mode. And I will search for snowsql. This one becomes snowflake. We start with use ro sis admin. If you read in the instructions or in the homepage of snowflake, different system

**[02:00]** roles have different responsibilities. And the sis admin is responsible for creating warehouse and databases. We use the role sis admin here. And if you want to use it, you execute it. You do control enter or command enter in Mac. Here it says the statement executed successfully. Here you can take a look into your role by doing select current role. This is a function. We run it and you can see that the current role is CIS admin. You saw before it was account admin. And you can also see down here that it says SIS admin and not account admin. Also you can do select current user to see which user it is right now. If I run this one you can see the current user is coion. Okay great. Now the CIS admin is responsible to create databases. I'll

**[03:02]** create a database ice cream. I'll call it ice cream db. We can do also like this. If not exists, there's a syntax error and that is because it should be like this. This one create database if not exist ice cream db. If I run this one, it works. This makes item potent. If I run it again, it still works. Okay, good. Then we can do like this. We can create a warehouse and then we'll create some tables to this database. Let's create the warehouse. We can do for example show warehouses. If I run this one, we can see that we have two warehouses here. Compute warehouse and something else. I want to create a new warehouse. create warehouse

**[04:01]** I'll call it warehouse where let's see warehouse sites equals to x small make it a small one extra small auto suspend I want it auto suspend after 60 seconds auto resume equals true Then I want to initially suspended this is true as well. Then I want to have let's say a comment equals to warehouse for development analysis. Warehouse for development and analysis something like this. Okay. If I run this one now, it says syntax warehouses. Let's see.

**[05:03]** Create. Oh, this one. Oh, I didn't have a semicolon here. I need to have semicolon. Then I run this one. Initially suspended. It's misspelled. Initially. Okay. Warehouse devwh successfully created. And we can show warehouses. And we can see this dev warehouse. If I scroll here, you can see a lot of different configuration for this warehouse. This warehouse we have created, we can see it in snowflake as well. Let's move on to snowflake to see that. Inside of snowflake, you go into admin and then warehouses and then you can see your dev warehouse here. The one that you have created and you can see it resumed 2 minutes ago. the owner is CIS admin because we used the CIS admin to create it and you can also in snowflake

**[06:01]** worksheets you could check show warehouses as we did in snowsql to see this warehouse let's move back to visual studio code and snow back to snowql we have here show warehouses and you can see the warehouses now it's time to create our databases or we have created our database create database if not exist it's time to create the tables And these tables I will just copy it for you. This I have taken from chat GPT. Nothing fancy here. Basically just three tables here. One with flavors, one with customers and one with transactions. Okay. Then I will run this one. I'll run this one. And I will run this one. To make this even more clearer, we could separate this into another file. Setup data warehouse dvh.sql.

**[07:02]** Here I can move out different things. I will move out this crate warehouse. I will cut this out. Put it here. And then I will clean this up a little bit. I will cut out. Yeah, I can copy. I will use CIS admin for that as well. I will take here a CIS admin. SIS admin we use for creating the database. We use it to create the data warehouse. Now it's time for us to create rolls. Here setup rolls.SQL. here. Let's change this change language mode to snow snowflake scale. Okay. Now to set up the roles, we use ro user admin. I will run this one. And now statement

**[08:01]** executed successfully. Then I'm in user admin. Now we can create different roles with this one. The user admin is responsible for user and role management. Create let's see create role ice cream reader. No, we'll give it a comment. Able to read ice cream db. That's fine. Then I'll create roll ice cream right there. If a comment able to do CRUD operations create uh read update and delete on ice cream dia okay semicolon semicolon I'll copy this one again create ro ice cream reader

**[09:02]** writer and then I will have an analyst as well analyst able to create views on ice cream DB. Okay, I'll run this one first. Then I'll run this one and then I'll run this one. Now I'm note that I'm user admin. The user admin have created these three roles. What we can do is we can check the roles show. And here you can see all the roles that we have. I have many more roles than you if you just started out. But we have let's see here the ice cream ones. We have ice cream analyst, ice cream reader, ice cream writer and they are not assigned to user. You can see zero. They are not granted to roles. You see zeros here as well. And

**[10:02]** you can continue to move here. They haven't they don't have granted roles. Remember you can grant roles to other roles. You can be granted other roles and you can grant roles to users. That is how it works. Now we have these roles and it's time to manage some grants. As we can see we don't have grants here. To manage the grants, we have the security admin that can manage all global grants. Use ro security admin. I'll run this one. And then we can grant usage on warehouse. We grant this warehouse dev warehouse to enroll ice cream

**[11:01]** in reader grant usage on this warehouse dev warehouse and we grant it to the ice cream reader. This means that this role can use this dev warehouse. Then we grant let me copy this again. Grant usage on database ice cream DB to roll ice cream reader that it can use this database. I'll run this one. Command enter to run it. Then we can do grant usage on all schemas. All the schemas in this database ice cream DB need to be granted to roll ice cream reader.

**[12:02]** Note what is happening here. Oh, actually I don't want to do that. I did auto format that's why it became a smaller letters. But I want to have this capital letters here as this is the convention grant usage on all schemas in database ice cream DB. Ice cream DB several schemas and for example we could have a staging schema. We could have a refine layer schema as these schemas they are granted to this role ice cream reader. They are granted to this role that it can access the different schemas. Then we will give some CRUD operations CRUD or we will give a read. The read is by doing select grant.

**[13:01]** Select on all tables in schema ice cream db. public. Note that it has a public schema by default and we want to give since we haven't specified any other schemas we will use the public one and we will grant select on all the tables inside of this schema. This schema public has could have a lot of tables. We have three tables now. Inside this schema, we want to be able to grant the select that the ice cream reader can do select on these tables. Ice cream reader write to ice cream reader. Now it can do select select to ice cream reader.

**[14:03]** So let's run this one. So we have select on all tables in schema. Uh so so now now you can read uh all the tables and then let's do rent um all tables uh in this public and we need to go in actually we need to we copy this one. So copy shift out down button. Grant select on all grant select on future tables as well to ice cream reader. So the the reason for this is that okay we have granted select on all um uh tables that exist in this public schema. However when we create new tables in the future uh they they are not granted. So when you when

**[15:00]** you need to do this grant select on future tables on this in this schema. Okay. So I run this one as well. Okay. It seems like a lot of the different things we need to grant in order to work with this and and this is to make sure that uh we have the right privileges. So uh prince p principle of least privilege. Okay. So future tables and now we can check the grants. So, show let's see, show grants to roll ice cream reader. And now you can see all the grants that ice cream reader has. So the ice cream reader role, you can see this is the grantee name, the one that has gotten this uh gotten this uh different privileges and what it has the ice cream roll uh re

**[16:00]** reader role. It has uh it has the ice cream name DB. So it has this database uh or it has the privilege usage on this database. It can do select on the tables that exist in uh in um ice cream DB public and the the tables are customers, flavors and transactions. These were the tables that we created before, right? And also it has this usage the warehouse the dev warehouse that we have. Okay. So now when we have this one, we can check take a look into show show future uh grants in schema ice cream db.

**[17:01]** public. So we can check take a look into the future grants and what we have is select and we have it on this uh let's see you can see we have ice cream db.public dot and here is the table so it means all the tables that uh will exist in public that it has grant on. So this is quite cool. Uh it's good to check your grants and then let's continue. Now we want to give so the the writer role uh should be able to read as well. So it's a role with higher access than the than the reader role. So we need to gr we can grant you what you can do is that uh you can grant the role reader to the writer. So we grant roll uh we grant roll ice cream

**[18:02]** reader ice cream reader to roll ice cream writer. So we grant this role to ice cream writer. And now you can take a look. Show brands to roll ice cream queen writer. >> And when you take a look into that one, you can see that it has usage privilege on granted role. So it has granted, let's see, let me scroll. Ice cream reader role granted to ice cream writer. Cool. So and and then uh if you want to know what type of uh usage privilege what type of privileges that uh uh means uh then you you take a look the show

**[19:01]** grants to roll ice cream reader. That is how uh this uh roll based arbback works in uh in snowflake. So with this now it's time for the right writer to get the the CRUD operations. So grant uh CRUD operations to ice queen right there. So then we do grant insert. We need to grant update. We need to grant delete because select it already has from the reader, right? And the reader doesn't have update, delete, and uh insert. Uh so on all let's see grant let me let me do like this on all uh tables

**[20:00]** in schema let's say on all tables in schema ice cream db public to roll uh ice cream writer. Okay. And then uh let's see. We run this one and then we copy this part because we need to do that on the future. So on not all tables on future tables in schema. Okay. So we do that as well. Run that one. And now when we have this uh we we do uh let's say we also want to have uh the ability to create uh new

**[21:02]** tables. So then you do grant create table on schema ice cream dbt public to roll ice cream writer basically like this. Okay. And uh and when you have done this you can um uh you can show um the you can do similar as before. So show grants uh show grants uh let's see what was the syntax for that. Show grants to roll ice cream. Let's see ice cream writer. And you can also check the future grants to it. But now you can see there's a lot of things. There's uh it has create table, delete, insert, update, delete,

**[22:01]** insert, update, delete for the different tables that exist, right? That's why there's so many here. And that is uh correct. And you should also take a look into the future grants. So we can copy this one. So show uh future grants uh in schema. So the the the the future grants you take a look into the database schema. So let's see in schema I screen db.public public and you can see all the future and you can see select delete insert update on this um on this uh schemas and uh and you can see uh which rows that have them. So you can see that ice cream reader uh it has uh it has this uh select and ice cream

**[23:03]** writer have the other ones for our crowd operations but ice cream writer also has the ice cream reader uh granted to it. So it means that ice cream writer also can do select. Okay. So what is left? Well, we can grant roll ice cream right there to user coction. But we we should use uh the uh use. So, uh let me change um use ro um user admin. Use ro user admin. Run this one. And now

**[24:01]** grant this role to uh to the user coction. So the coction has this user. So has this role uh has the ice cream writer role which means that coction also has the ice cream reader role. Uh so so uh this is uh how uh now now if if you have several users you can make sure that one user will get uh the uh the ice cream writer role. And for exercise sake you should uh implement the analyst role as well. I haven't uh implemented it here. And now let us uh take a look into the tables and data in uh so we will take a look uh tables and data SQL. So change the language to snowflake. And here in order to use this one

**[25:03]** uh we can do like this. Uh so so we did setup DB before right. So use roll ice cream writer use warehouse dev warehouse use uh schema ice cream db.public public. So I'll use this role. I'll use this uh the warehouse and I'll use this schema and then inside of this schemas we can create the different tables. So I'll just copy this one. Uh but however we have already uh created the tables before but uh this should work as well.

**[26:02]** But it says that flavors already exist. Uh so we can do uh we could do create uh we could uh drop the tables maybe. So drop table uh because I'm using the schema I can just do like this flavors. Okay, I drop this uh table. I will drop the next table which uh which is that customer customers. I'll drop that one as well. And I will draw the last one is transactions. So I'll drop transactions as well. Okay. Now I will run this one. So create table. I could do if not exist also but uh let's do that. Create table. Create table. Create table. Okay. So now I created three tables. And this is also from chat GPT. So I will just insert some data. So insert uh flavors.

**[27:03]** insert some customers and insert some transactions as well. Okay. And now we could do we can take a look into this. So select star from flavors for example. And you can see this is the data we have. We have flavors price etc. And uh it doesn't matter that this uh we haven't uh done any data modeling on this data. This is just raw data from chat GPT. Uh so we can take a look uh let's see see if we can uh use the role we change the role to ice cream reader. So you can see that the ice cream writer roll could do a select. So how about the ice cream reader roll? What can it do? Well, we can do select star from

**[28:00]** customers. That's no problems. Right. Yeah, we get the customers. However, can we do like this? Can we insert a new item into customer? So, I want to insert this one. So, if I run this one, it says number of rows inserted. The thing is that we are in the reader role and in the reader role, we shouldn't be able to insert this. But the reason that it is possible to insert it is because if you take a look into SnowSQL, you see here, you scroll down a little bit that you have the ice cream reader role and there's this use secondary role. It means that if this one fails, it will use the other roles. If we remove this one and we run this again, now you can see there's an error because we cannot use the secondary rule roles. We only have ice cream readers. This is just how when we have snowql in visual studio code it's designed in this

**[29:01]** way that you have the secondary roles. You can take a look into secondary roles. It's all the roles that we had used before. Basically what it does is that the studio code extension by default it enables the use of secondary rows. Check below if there are any active second rows. What you need to do is to check this current secondary roles. and you run this one. Let's see like this. Now you can see there's no roles here. However, if we tick this one and you run this one and you can see ah there are a lot of secondary roles. This is very important but this is something to look out for. The reader role we can do select star from transactions for examples as possible. Now get you see it here. One thing is that we can take a look into snowite.

**[30:00]** Inside of snowites, you go into databases and you go into this ice cream DB and you can see here the different information schema that exists for all metadatas. We have public and under public we have tables. We have customer flavors and transactions. And you can take a look into customers. Here there is columns here which columns we have and the data preview. You can see John do came three times because of the error before or the secondary roles. But you can take a look into all the data here. The reason why we can see it in snowite is because we have granted a role. We have granted the role writer to the user coction. I'm the user coion. I can see this cool. It's quite fun that we can work with snowsql. You can see that it actually works in snowflake. In this video, we have gone through how to create the different roles. We have created a warehouse. We have created a database and granted different access and different privileges to the roles and

**[31:01]** granted roles to roles or granted roles to user. We have also worked with inserting some data and tried this out. It puts the foundation in this course. So it's very important that you learn how to work with roles and roles management in snowflake because this is the foundation for later on when we come into for example and when we're connecting stream to snowflake as well a lot of things you need to have the right privilege and you need to work with something called PP so principle of least privileges this is quite important concept thank you for watching this video and see you in the next one bye. Okay.
