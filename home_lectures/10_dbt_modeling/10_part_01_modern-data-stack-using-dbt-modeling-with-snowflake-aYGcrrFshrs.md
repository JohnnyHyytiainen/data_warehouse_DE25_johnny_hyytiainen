---
title: "Modern data stack using dbt modeling with Snowflake"
video_id: aYGcrrFshrs
source_url: https://www.youtube.com/watch?v=aYGcrrFshrs
language: en
transcript_type: auto_generated
fetched_at: 2026-09-21T15:24:30+00:00
source_type: youtube_transcript
---

# Modern data stack using dbt modeling with Snowflake

**[00:00]** Hello and welcome to this video on DBT modeling. This video is based on some previous videos. One is the DT video where we've loaded job tech data, job ads data into Snowflake. There we also set the users. We set up the user for DT. We set up different roles for DT that it can access the staging layer of our data warehouse. This is based on first this one and then it's also based on another video where we've talked about setting up DBT. And when we set up DBT, we also did some worksheets in Snowflake where we created the transformer user. We created the job ads dbt ro and then we also made sure that we could connect to snowflake through our profiles.yaml

**[01:01]** and our dbt project that we can do dbt debugs that it works towards snowflake. Based on this we will continue on after we have done this setup. Also in the setup we set up DBT power user in Visual Studio Code. Now when you have all this set up it's time to continue to create our DDT models. Then we will have based on our dimensional model which we also have covered in another lecture. However, I will show you that image that we will follow that one and I won't do all of the models. Instead I will just do one fact, one dimension, one marks and then because I don't want to take away all the fun for you. I'll just show you all the core principles and then you can go on and do all the fun yourself. Let's move on to Visual

**[02:01]** Studio Code. Now here I am in Visual Studio Code. What I have is I have a virtual environment. I have activated it and I have installed I've done uvp pip install dbt- core dbt- snowflake and DT since we will be using DT to load some new data as well that is what I have and then we have here I will go back a little bit worksheet snowflake what have we done here we have in the previous video where we've gone into DB BT setup we needed to we also did some setup in our snowflake. What we did is set up the user here. We created this user and then we set up the warehouse. We set up we granted the user these different roles job as DBT role and then we did set up the MRS layer. We did set up the warehouse layer as well since the

**[03:02]** staging layer we have already set up before. What we do here is that we just set up these two layers. Now we have three layers. We have our we start with the staging layer and then we have the warehouse layer and then finally we have the marts layer. These layers are basically just schemas in in snowflake. Okay, we have done this setup and then we also have this job ads dimension model. This model here you can see it's a star schema and this is a quite good model in order to to make it understandable to get the mental clarity of the of the models that you want to create that you can get mental clarity of the data. Basically we have the fact in the middle and then we have dimension occupation. We have dimension job details. We have

**[04:00]** dimension employer and we have auxiliary attributes as dimension. These are dimensions that will enrich the fact that you can see the fact as an association table or some kind of bridge table where you have the a lot of foreign keys to the primary keys here. They're connected through these and then there's also a few facts here that are the vacancies integers and relevance in float and also the application date in the fact job ads. For example, you want to know more about the job details about this particular job then you just join this one and you get all these columns as well. That is the idea and then you can choose what to serve for different marts in the marts layer later on. Okay. And we also want to go through in the DBT code. What we have here is the DBT project YAML. Here we have set the this is the

**[05:01]** name of the project called DBT code. And here we have the profile is the DBT snowflake. This must match the profile in your profiles.yaml. You reach the profiles.yaml by doing this. Open tilda /dbt/profiles.yamel. If you click okay there, I won't do that because if I do that, you will see all my credentials and that's very bad. You should open this one and you should change your credentials and you should have one that is called dbt snowflake. And note that if you are for example following this course, you could call it dbt snowflake_c course if you want or whatever but it must match the one in your dbt in your profiles and then you could have one that is called dbt snowflake project for example and then you need to match that in your yaml as well. You

**[06:00]** kind of several profiles in your profiles.yaml YAML and inside of profiles you have your setup. You have the password to your account identifier for your Snowflake account and you have the user for example the transformer user that we have created before with a password for that. You have created this user in the previous video in the DBT setup. I just wanted to point this out that this needs to match. And then scrolling down here you can see that right now we have the DBT code we have the staging and we have the refined here we have staging for is called is the staging schema and refined we have the warehouse schema here if you have models here you have staging part and you have a refined part here you have the staging basically you have the inside here you have where it's connected to the warehouse schema

**[07:02]** in your snowflake or in your data warehouse and the staging folder. All the models here, they will connect to the staging schema in your snowflake account. Okay, I hope that this was not very hard to understand. If you think this is hard to understand, go back and make sure that you follow all the other steps in the previous videos first. I will link all the previous videos in the description. You can take a look into that. And let's go in here. Let's show the DT as well. Here the DT you need to put in your secrets for DT. There's not DT inside there. There's secrets. I won't show that. But in our repository, you go into the DT API part. Then there's how you set up the secrets and also in if you're not sure about how to set up the DBT the profiles at YAML in DBT you go

**[08:02]** into the setup DBT folder in our repo. I have everything in the repo. I have divided into several lectures here in the load job ads. This is a little bit different from what we have done before when we loaded extracted and loaded tech API. Before we did with just a query data engineer and now the difference is that we want to have technical jobs. What we have done is here you have a particular occupation field with this ID here which you can search up for in the documentation. But based on these you will get the technical job ads and this is the first one that I will start with in this lecture here. Everything else is the same. It works by doing this. You do a get request basically and you have a resource here where you yield each ad

**[09:01]** and then you have this in a pipeline and you extract and load the data from the API. You load it into the staging layer or the staging schema inside of Snowflake. And when you do that, you do this pipeline run job ads resource and you have this table name that's technical field job ads. Before we had something called data field job ads. You should know that. And this OS change there is just that you are in this folder that you can see this D. DLT that is it. I will run this one first. Start running it. You can just click the run button if you have activated your virtual environment down here. It takes a little bit of time to run this one. It's loading the data. And here it says yes, it's done. Load step took much time. Load package is loaded and contains no failed jobs. And when it has done this, you should go into Snowflake to take a look into your data. We'll take a look. Now inside of

**[10:01]** snowflake we have here we start with data field job ads. This is the previous that we have loaded but now we have loaded another one called technical field job ads. This is what we just loaded and you can take a look into DT loads for example and data preview. You will be able to see that we have just loaded here 0907. This is the most recent one. So this is UTC time. It's 2 hours behind our clock. Now we have technical job field ads. This is the data that we have loaded. You can see data preview here. And you can see here is the data. And you can query this if you want. Good. Now we know that this data has been loaded. Let's move back to Visual Studio Code and DBT to do some modeling. Back in Visual Studio Code, let's start with something very simple. We need to go into cd dbt code. Now I'm inside of dbt

**[11:04]** code which means that I can see my dbt project.yaml. If I do ls I can see dbt project.yaml. And now we will just do dbt debug to see that everything is connected correctly. Yes. And you can see that it works. If it's not connected correctly, make sure that you connect it correctly. I'll show you in my GitHub how the connection should be inside of setup dbt in my GitHub repository. You can see in the readme here that it says in the profiles.yaml which you find in this directory home directory. DBT/profiles.yaml. You have here this is what you should put in. This should be connected to the same as the ones in our DBT projects.

**[12:04]** Then you have here target dev. You have outputs dev and then you have snowflake and this is the user transformer. This is the password that you have used when you created your transformer user. This is very important. They must match. And then here the account identifier. You find it inside of snowite. You go into your own account and then you find the account details and insidider account identifier. You fill this in here. And then when this is done, let's move back to Visual Studio Code. When you do dbt debug and it seems everything has checked, then it's fine. You're good to go. Let's start. This is a copy from the 10 where we did the setup. But I will not need refine or staging. I will remove this. Actually, I'll remove this one as well to make it a clean start. In the models, we will create a few folders. I will go into CD models and here I will create

**[13:02]** dim, fact, mark and source. These are four folders that I will create here. And the reason for this is that this is how I want to structure my DBT models. I want to place all the dimensions in my dim. I want to place all the facts in my fact and all the marts inside of Marts and source is the ones that will connect to the staging. Here we will have something else here as well. I'll show you later soon. However, let's take a look into our dbt project.yaml. This is very important that you have here is dbt code. This is the same as the project name. Your project name here is DBT code. It should be the same here. And staging in schema staging. Yes. Materialized as here. I want this I want it to be called I don't have a staging

**[14:02]** here. Right. I want to change it to source. Here we changed it to source. And actually I can delete all this. We don't need that source. And then we'll do plus materialized. I want it to be materialized as ephemeral and then I want to have plus schema staging and this plus is just to denote that this is a resource config and this is somewhat is backward compatible to older versions of dbt but I like to use the plus in order to denote that this is the resource config and it means that there's a lot of configurations we can do here for the resources that is how dbt should build or run these models and where it should be stored, how it should be materialized. Ephemeral means that it's a temporary table. We materialized this DBT the models in the source as ephemeral that you can just use them

**[15:00]** there but it's not stored in snowflake at all and that is the idea and the reason for this is that you have this source for the reason that you can actually you can do testing on your source data and that is really useful you can test to make sure that this the data from the source is is validated before go moving on that make sure that your pipeline doesn't break that you know you should test it in different places that is the idea with the source as well and here I want to make it inside and I want to materialized as table in form more general this means all that are not specifically decided how it should materialize will be materialized as tables okay and then we have dim the dimensions options should have schema and I will choose the warehouse schema. This one we have set up before and then we have fact it

**[16:00]** should be in schema let's see warehouse as well we note that staging is the landing zone where all the data will land and then afterwards we do the dimensions and we do the facts in the warehouse layer where it's the more the refine layer usually you can call it warehouse you can call it refine layer now we have picked the name warehouse we're stuck with that one but that's totally fine And then we have the marts layer. And here we have plus schema. It's inside of marts. Very importantly is that these must match the names. Here you have dim fct marked and src. You have src dimact mark because here our dbt models will reside. We will have our dbt models here. And then you can see the actual schema here is the schemas inside of snowflakes that will be changed. And note that this is just I'm just talking about Snowflake right now, but it could easily be

**[17:01]** something else. For example, BigQuery, it could be duct DB. It could be something else. What you need to change is your DBT, the profiles.l that you you choose here that you're pointing your profile to something else. Yes. And that is it. I think dbt packages. Yeah, that is totally fine. And yes, I'm happy with this one. DBT debug to see that it still works. Yes, it works. Clear this one. And now let's go into our source and we'll start with creating something called this is our starting point of our journey for DBT modeling. sources you define here in this YAML file dash name job ads and then here let me do spaces here

**[18:00]** schema is staging here we'll pick our data and I'll explain why we do this tables and inside of tables we have dash name staging ads And we have identifier is technical field job ads. Okay. Remember the name of the table is technical field job ads. This is the name of the table. Okay. And then we have we give the name here stage ads. And then you have the schema is inside of staging schema. This is where we find the data is this schema and this table name and then we have named it stage ads and we have named this job ads. The reason for this is that for example it could be changed in your in your staging data in your staging layer. The this name could be changed and the

**[19:02]** reason for with doing this in the sources.yamel is that when it changes you just need to change in one place. You just need to change in this file here, this YAML file. And all of your other models, they don't need to change at all. They can be the same. Otherwise, if the name changes here in, for example, in Snowflake, someone has changed the name of this table and then you need to change all your model names where you refer to the data and that is quite tricky. You want to change it in one place. Good. Now you have sources. And we will create our src job ads. SQL. And we will create. Yeah, we can start with this one. This is a dbt. You can change language mode. And you can choose here DSQL. You have this SQL and you have configured it to be dbt. DBTSQL.

**[20:04]** Start with note that this is an extract of the model. It's not the whole model because I don't want to take away all the fun for you. Okay. So, let's start creating this model. But first, you need to change to the right language mode. So, shift control P or shift command P if you're on Mac. Change language mode and you choose here SQL. So, you click this one ging. And when we choose this one, we will have the power of power user. So, this is really cool. So with stage job ads as select star from and now let's see this magic ref and then enter. Cool. So now we will see here ref and you choose the model name that you want or actually I don't want ref. This is not the ref I want. I want to have a source actually. Source. Enter. Oo this is so cool. So we pick

**[21:02]** the source and the table name. Okay. So let's take a look into sources.yamel. Source is the job ads. So this is the name. And then we have here name of the table stage ads. Okay. So source job ads. That's it. And here we have stage ads. So remember what I said before. So here it means that we have so whenever if we change something in the snowflake table name we just need to change in the sources because everything else is that is connected to the sources is is done through this one. So you have this CTE here and you have this source job ads and stage ads but these stay the same. Job ads here the name here and this name stage ads stay the same and the only thing you need to change is this here and or this.

**[22:02]** So that is very cool. Okay. So we have this one first and then select and here we can just select a few of them. For example, we can start with select star from stage job ads. And let's see stage job ads because it's this CTE that we're working with. And let's start with this one. So, Ctrl enter. Yes. And the DBT power user is run. Note that the power user is not needed. You can do this directly with dbt run and you can see the results in in snowflake unless it's ephemeral that it is in this way in this case. However, the thing is that it's quite good to have dbt power user so you can see the query directly. However, this could get stuck sometimes but it's let's see. Yeah, maybe it's gotten stuck. So, it seems to be this error here. DBT found one packages

**[23:02]** specified in packages.yammo. YAML but only zero packages installed in dbt packages. So let's do that. DBT deps. Actually I'm in models. Can I do this? I need to go backwards. See the DBT code. DBT depths updates available for this one. Update your versions in packages.yaml. Then run dpt depth. So basically it works now. And what I did was okay, I just changed I went into my dbt the packages.y and I changed it to 1.3.0 because that was what the error stated or the message stated that there was a newer version of this one. Then I did dbt deps again and updated it. But still dbt power user doesn't work. But then I just restarted Visual Studio Code. So close it down and start it again. And now it works. So run it and

**[24:00]** you can see run it with command enter or control enter if you're in Windows and you can see here is the data and the thing is that it uses snowflake as the compute to do this so that you can uh so when you get the data here you can find out which one that are interesting for you and the things that I think interesting is that if you go into job ads dimension model here and then here we will need a few things. So we will need for example the relevance right? We will need the vacancies here. We will need application deadline and we will need some other things. And the thing is I will pick this and create based on this fact job ads. I will create this source because later on in the fact job ads it will use this source job ads. So then we will create here. So we will have occupation

**[25:02]** underscore label we will have number of vacancies. So these are actual columns here and we will have this as vacancies. We'll have relevance. We will have application deadline. Also, one other thing that you do with source job ads is that you're already filtering out which type of columns that you want to have for your downstream models. So here is upstream very close to the s very close to the actual source of the data in the staging and afterwards you serve the downstream which is in the refine layer or the warehouse layer. So remember this is an extract of the model. It's not the full model maybe. And here you can see these are a few things that we get. And then we order by application deadline.

**[26:03]** And you can see the application deadline here. Yes. 0910. Okay. So let's move on to our next source. Create another one. src occupation.sl we should change this one as well to gaql good now let's create this one and similarly we create this one so with stage job ads as select star from source and then enter and here you pick your source so job ads And here you have stage ads and then select and here you have the different things from stage job ad. So this is the CTE name here and now we'll

**[27:00]** pick occupation occupation group concept ID. So now what this is source occupation is basically if you look into this here is that we have this dimension for occupation. So we need to get all those that are relevant to occupation and place it into source occupation and then we use this later on to create this dimensional model. Okay. So we have here concept ID as occupation group ID and we have here occupation. Let me just copy this occupation group. The occupation field concept ID as occupation field ID. I will copy this again.

**[28:00]** Occupation label it's like this as occupation. So simplify it as this. And then we have occupation group. Let's see occupation group. Occupation group label. And in order for you to find this, you should take a look into the data and see it side by side. What type of columns can we pick? So this is detective work that you need to do. So I already done this. So that's why I know which one I need to pick out as occupation row. And then finally we have occupation field label as occupation

**[29:01]** field. Okay. So I run this one and you can see yes occupation group ID occupation field ID. occupation, occupation group, occupation field. So these are the five that we've picked and we have renamed it a little bit here to make it simpler. So that is what you can do with the source models. You can do some simple renaming and then afterwards you have these models which you can build your dimensions and your fact. So let's move on to the fact here we have fact job ads dossql change the language mode to SQL but ginger SQL okay

**[30:00]** with job ads as select so you always start with a CTE in DBT models so select star from and now we don't take from the source we take from reference and the reference we choose the source name basically the model name so this is so beautiful with dbt that all the references all the sources it's based on the model names which if you change the model names you need to change this as well however it doesn't have the the hardcoded references instead the when you compile the code into the target you will have the hard-coded references. So it's okay select star from this actual table name where you have to go into this database the staging this layer and then this table it's fixed in the target where you have this the outputed SQL file which comes from comes from all the

**[31:02]** compilation of your DBT models which you do when you do dbt run. And I'll show you that later. Ref source job ads. So we take from this source job ads. So this is the upstream model that we pick from and we do select and here we will do like this. DBT_utils dot generate will actually do like this. So this is how you pick the some functions and we pick dbt_you utils dot generate surrogate key. So this is a macro. So ginga means that ginga sql means that it's SQL combined with macros and macros

**[32:03]** are basically just functions. And this function generates surrogate key. It's in this dbt utils package. That's why we installed this package before. And what it does is that it generates a surrogate key. So it's here we want to create this surrogate key and it should be based on a column name. And we can go back to source job ads and you can see this is occupation label. I would pick this one. as the column name and we'll put it here and this means that this one will be the surrogate key and here what does surrogate key means let's see as occupation ID so it's basically just a key that we have created based on previous columns which we have done some processing to so

**[33:00]** let's see here from job ads so this is the job ads I'll run this one and you can see here. So this is the surrogate key. It is a hashing function. Let's see what we'll see here. And you can see the raw function here. The raw function is MD5 cast occupation label as text. Okay. So what this does is that we are casting the text from this column name into MD5 format. So it's a hashing function. So we hash this column basically. But importantly is that when you do hashing function it means that one input will give one output. It will always be unique in that way. Tables as well or other dimensions you need to do the same columns in order to have it linked to the right row. Right? So this is quite long name that's why I renamed it using an alias here as

**[34:00]** occupation ID occupation ID that fits very well and then we will continue here with vacancies. I will pick that one and I'll pick relevance. I will pick application deadline as well. So these are the things that I will pick from my source job ad. So if I run this one it will looks like this. And note this is also an extract of the whole model. So it's not all of them. So you can see here we have occupation ID, vacancies, relevance and application deadline. Okay. So that is the fact and we will go further to the dimension here and here I will have one dimension. So dim occupation occupation.sql and here we need to change the language mode to SQL. I'm going to ginger SQL here. So same as before with source occupation as select star from ref and let's see

**[35:01]** ref going down this one you should click for power user and ref model name we go into source occupation so this CTE here we can use the source occupation so select let's see select here from source was occupation and what do we want to select? Same here. DBT utils dot generate surrogate key. And here I want to use my occupation. Let me choose occupation. You can see source occupation. You have here this occupation label. I will pick this one, the one for doing my key. or my ID. So as occupation ID so this is my primary key and in my foreign key I will connect to this one.

**[36:00]** Okay. And then we'll pick occupation as well. This is quite special. If we pick occupation group and we'll pick occupation field. So if I run this one will come soon. Yes. And occupation group. The thing is that you can see there's here occupation group and then we have the same again. But the thing is in the dimension what you want to do is make it unique. And in order to make them unique what you can do is group by the occupation. So you group by this occupation. then this will be unique. And then when you group by you need to have an aggregation function and we do max here. So it will just pick the top one. So max of this one as occupation

**[37:02]** field and as occupation grew. And if I run this one now it should be fewer rows. Exactly. And here you can see these are unique now or the unique thing is from the occupation here. So you have all the unique occupations and then you take the top from each of these ones. So that's why I use max here. Okay. So now we have our dimension occupation and it's time for us to go even further. We want to go into our marts. So this is the downstream of the warehouse layer. So the refine layer or the warehouse layer and then we have the marts layer.

**[38:02]** And for the mart layer, we'll create a file called mart technical jobs.sql. And note that you might have several more dimensions. And according to this dimensional model here, we have more dimensions, but you should do that, not me. So change language mode. Sla. And here we do with. Now note that this is the next step. So we take fact job ads as select star from ref. We'll take this from our fact. So fact job add. So this must be same as this name. Deem occupation as select star from refe

**[39:05]** occupation occupation. And now we do select. Let's do select star first from fact job ads as f and then you can join what you want. So for example here we can do left join in dim occupation as O on F dot occupation equals to O do occupation ID. So note that both had occupation ID. So if you look into dim we have

**[40:01]** occupation ID. it when it used this occupation here and fact job ads it has occupation ID it used this occupation label they are the same because we had source job ads we have occupation label here and we had this source occupation it's occupation label but I maybe I shouldn't have changed the name to occupation here but that I've already done that now it's fine It's still the same column. Note that source occupation refers to this occupation label. Source job ads refers to this occupation label. So it's the same column. I just mistakenly changed the name before. But now when I've done that, I don't want to change it back. So in the marts you can see we go into f occupation ID and o.ccupation ID. Why is

**[41:00]** this possible? Because demo occupation we have changed it to the occupation ID fact job as we have done it to occupation ID and since we are doing surrogate key for these the same column it will give the same ids and we can join them. So we join this and let us run this one and see what happens. So here we get occupation ID, we get vacancies, relevance, application, deadline. So these are from the F. So fact, job ads. And then we have occupation ID 2. This is from the O and occupation I occupation etc. are everything that's related to occupation. That is very good. So we have joined this together and then we can pick the things that we want. F

**[42:01]** dot vacancies F dot relevance O dot occupation O do occupation group O do.occupation field F.lication deadline. So now I pick the things that I want here. So these are the things that I have now. Maybe I can take occupation first. And these things the marks are the things that are sent downstream. So it's sent downstream so that we can connect this with for example streamlit. We can connect directly to marts. We can connect for example taipai to marts. We can connect the other front end to marts maybe powerbi tablo

**[43:00]** and the thing is these different marts you give access to them based on different departments different people so you serve data but you don't serve all the data you choose the data you want to serve to the specific person or the specific entities so that is the idea with the marts and here we have if we want to more technical jobs we could also So do like this where occupation field equals to this uran technis inning. So we just take out these these jobs for example. Maybe all the jobs I picked already had this only had this technique. But if you're ingesting several more jobs then then definitely you can filter this. Okay, now it's super fun. Let's try to materialize this now. So before we do

**[44:02]** that, we go into this generate schema name. It is very important that you have this script here because this one will make sure that it will enter into the correct names in snowflake. So I won't explain exactly what this does though. This is just a macro that I've taken from the documentation of dbt. So what you do now is to materialize. So you do dbt run. Remember you're inside of dbt code and you do dbt run. Dbt not found. Right. I haven't activated my virtual environment. I go out dbt modeling. Yes, this is true. So source van bin activate cd dbt code dbt run and it found five models. So 1 2 3 4 5. But it only ran three. Why is that? because we don't materialize

**[45:02]** the sources. They are ephemeral. So this is totally correct. So we have 1 2 3. So now let's move into snowflake. So inside of snowflake in job ads database we have these four schemas. So we have the mart schemas, we have public schema, staging and warehouse. Okay, take a look into staging first. And now this is not strange. These are just the data fields and technical field that we showed before which we gotten from DT, right? So nothing else here we haven't because our resources are ephemeral. Okay, they don't materialize into tables in snowflake while the other ones becomes tables. So we take a look into warehouse. This is our next layer after staging. So here we have tables and we have okay this updated headline and

**[46:00]** updated headline micro this is just from a previous test in the previous lecture we had this however this deem occupation and fact job ads you can see here fact job ads data preview and you can see the data is here occupation ID vacancies relevance application deadline team occupation Data preview. Yes, it's here. You can see occupation ID, occupation, occupation group, occupation field. Great. And then take a look into marts mart technical job. So these names are the same names as those that we have picked for our SQL models. Click on this one data preview. And this is the data that we're serving. So this is the data that now we can choose who should get access to this mart technical

**[47:00]** jobs. Maybe only the technical department will get this or maybe the marketing department we choose right and we have several marts that we can give out to different departments. So you can see here occupation vacancies relevance occupation group yes it's here occupation field etc. So cool. We get we gotten the data inside of Snowflake after we had done the DBT modeling. So this is so good. Wow. I will just say wow. This was a very long video but totally worth it. We have used DT to extract and load the data from jobtech API into staging layer of snowflake. And then inside of the staging layer, we have used DBT models to do we have created several DBT models to do the modeling. And then when we have done the modeling, we do dbt run. And all the models that we want are materialized inside of Snowflake. And

**[48:00]** then we have the data in divided into the layers that we want. staging warehouse layer or the refine layer and the mars layer where we have served our data and this is uh note that I didn't do the whole modeling I did just very simple part so that you can use this later on for your consume it for your later applications for example using streamly using typi using react etc or you can use whatever tool you want you can use powerbi tablo and later on show the beautiful dashboards to your different stakeholders. I hope that this was worth even if it was a long video, I hope that you stayed and fought with me and learned a lot. So, thank you and see you in the next video. Bye.
